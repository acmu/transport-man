import React, { Component } from 'react';
import withLayout from '#components/withLayout';

class UserInfo extends Component {
  render() {
    return <div>this is UserInfo</div>;
  }
}

export default withLayout(UserInfo);
